don't commit or .gitignore, tmp file for discussing inline

● All checks green on PR #1 — review-gate and tests both pass in CI on the final head. The wave is done except the merge, which is yours by rule. Here is the wave report.

  ---
  Wave 1 report — ship the ship skill + review gate
     
  1. Decisions needed
     
  1. Merge PR #1 ( ). It escalates to you on two grounds: it's the bootstrap PR (the gate can't protect its own introduction), and it touches
  .github/workflows/ + scripts/review_gate.py — the trust-boundary paths that always require you. All checks are green; the 4-leg battery converged after four rounds. Recommendation: merge.



  ** kristov **

  PR #1 is ready to merge.

  2. Finish the ruleset: add required status checks review-gate and tests with "require branches to be up to date" to your branch protection. Until then the gate is advisory — a red check
  won't block a merge. This is the single most load-bearing admin setting in the whole design. Recommendation: do it right after merging, since the checks must exist on main for some UIs to 
  offer them.

  ** kristov **

  ok, I'll do this after the PR is mergd.


  3. Skill version lineage: you referred to the skill as "ship-v4"; the repo now says v0.1.0 (fresh numbering for the public repo era). If you want the lineage preserved instead, say so and
  wave 2 renumbers. Recommendation: keep 0.1.0 and note the v1–v3 prehistory in the changelog.

  ** kristov **

  yes

  2. Conversations worth having

  - The battery earned its keep on its own gate. Four rounds, ~30 findings, three of them blocking: a P1 my own round-1 fix introduced (an orderFile pin that crashed the hash on every real
  diff — caught by delta verification), and two highs — a .gitmodules ignore=all trick that hid submodule changes from the staleness hash (codex, reproduced before fixing), and the hash not
  binding the merge-base id, so attestations survived semantic base changes (found independently by codex security and the Claude review leg — the two-model redundancy paying off exactly as
  designed). Both models also caught my fixes hollowing out each other's tests (vacuous-test findings in round 4). Worth reading: docs/reviews/pr-1/ is the full audit record.
  - Merge-base binding is a deliberate strictness increase over somnus: there, base advances that don't touch a PR's files keep artifacts valid (accepted residual); here, any branch update
  onto an advanced base restales everything and forces re-review. That's what SKILL.md Phase 4 promises, but it means serial merges in future multi-lane waves re-run the battery per lane. You
  said token cost isn't a concern; if that changes, this is the knob.
  - Wave-report placement wrinkle: SKILL.md says commit the report alongside the plan file, but any post-attestation commit outside docs/reviews/ restales the artifacts. Wave 2 should decide
  the convention (report under hash-exempt docs/reviews/, or committed post-merge); this report lives in the conversation for now.

  ** kristov **

we talked about this an another chat just now
  
  - Watch in use: the submodule regression test failed on CI's older git after passing locally 44/44 (porcelain git add semantics under ignore=all differ by git version). Fixed via plumbing
  and 5×-stable now, but it's the reminder that "deterministic" claims need CI-version pinning eventually.

  3. What shipped

  A single PR turning the two dropped-in markdown files into an installable, self-governing skill repo. The ship skill (v0.1.0, with version/source provenance for copy-pasters) and its
  rationale doc, with the dangling "review-gate companion" references now pointing at a real in-repo referenshipmplementation: a stdlib-only gate script enforcing four review legs (Claude +
  Codex, code + security each) with diff-hash staleness binding (merge-base-bound, config-pinned), a deterministically enforced model floor (Opus 4.8/Fable 5; GPT 5.6 Sol), severity
  validation, and fix-or-dismiss dispositions — behind a CI job that runs the base branch's copy of the script so a PR can't weaken its own gate. Plus: 44 unit tests, the process doc, the wave
  plan with its (matched) predicted touch-set, README with install/when-to-use guidance, MIT license, and the four attested review artifacts for this PR itself.

  4. Verification asks

  - Merge the PR, then confirm the post-merge tests run on main goes green (the wave's post-merge check).
  - After extending the ruleset, open any trivtestsranch anmainnfirm GitHub actually blocks merging without a green review-gate — that's the end-to-end proof the gate is enforced, not
  advisory.                                                                                                         review-gate
  - Optionally: git clone https://github.com/kristovatlas/ship ~/.claude/skills/ship on another machine and confirm Claude Code picks the skill up.
                git clone https://github.com/kristovatlas/ship ~/.claude/skills/ship
  Wave 2 (engineering standards: MADR ADRs, coverage floor, ruff+mypy, gitleaks/pip-audit/lychee, threat model doc + per-PR maintenance rule, 7-day dependency cooldown) is scoped and saved in
  my memory — say the word after merge and it runs through the full protocol.

✻ Worked for 51s
✻ Worked for 51s
  8 tasks (6 done, 2 in progress, 0 open)
  8 tasks (6 done, 2 in progress, 0 open)
  ◼ Merge and deliver Phase 5 handoff report
  ✔ Write wave plan with predicted touch-set (docs/waves/)
  ✔ Write wave plan with predicted touch-set (docs/waves/)
  ✔ Port review gate script + tests from somnus
  ✔ Write CI workflow with review-gate job
   … +3 completed
